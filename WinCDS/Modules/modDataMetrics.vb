Module modDataMetrics
    'Public Sub TrackDataAccess()
    '    Dim N as integer, D As Date
    '    TotalQueries = TotalQueries + 1
    '    If MetricsINI = "" Then Exit Sub

    '    D = DateStampValue(INIRead(DataMetrics_Section, DataMetrics_Date, MetricsINI))
    '    If IsNullDate(D) Then
    '        D = Date
    '        INIWrite DataMetrics_Section, DataMetrics_Date, DateStamp, MetricsINI
    '  End If
    '    If Not DateEqual(D, Date) Then INIWrite DataMetrics_Section, TrackDataKey(), "0", MetricsINI
    '  N = Val(INIRead(DataMetrics_Section, TrackDataKey(), MetricsINI)) + 1
    '    INIWrite DataMetrics_Section, TrackDataKey(), N, MetricsINI
    'End Sub

End Module
