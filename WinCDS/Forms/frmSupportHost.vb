Public Class frmSupportHost
    Public Sub Listen(Optional ByVal DoShow As Boolean = True)
        'sbc.Listen  --- sbc is name instance of custom acticex control named "ScreenBroadCast" control. It is not developed in vb.net.
        'sbc.Visible = False
        If DoShow Then Show()
    End Sub

End Class